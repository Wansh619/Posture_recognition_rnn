def avg(skeleton_points):
    sum = 0
    a = len(len(skeleton_points))
    for i in range(a):

        sum+=len(skeleton_points)
    return sum/a   

    # sum = 0
    # mn = 10000
    # for feature in FEATURES:
    #     sum+=len(feature)
    #     mn = min(mn,len(feature))
    # # print(sum/len(FEATURES))
    # return mn


